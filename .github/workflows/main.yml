on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to check stuff
    steps:
      - uses: actions/checkout@v3
      - id: foo
        uses: check_github_actions@v1
      - run: echo "wrapping up"
        shell: bash
